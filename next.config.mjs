/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["corkcicle.com", "i.pinimg.com", "avatar.vercel.sh", "cdn.prod.website-files.com"], // Tambahkan domain ini
  },
};

export default nextConfig;
