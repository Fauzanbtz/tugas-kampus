/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["corkcicle.com", "i.pinimg.com", "avatar.vercel.sh"], // Tambahkan domain ini
  },
};

export default nextConfig;
