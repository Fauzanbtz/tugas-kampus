/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["corkcicle.com", "i.pinimg.com", "avatar.vercel.sh", "cdn.prod.website-files.com", "instagram.fcgk12-1.fna.fbcdn.net"], // Tambahkan domain ini
  },
};

export default nextConfig;
